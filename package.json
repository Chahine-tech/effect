{
  "name": "myapp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @myapp/api --filter @myapp/web run dev",
    "dev:api": "pnpm --filter @myapp/api dev",
    "dev:web": "pnpm --filter @myapp/web dev",
    "start": "pnpm --filter @myapp/api start",
    "db:generate": "pnpm --filter @myapp/api db:generate",
    "db:migrate": "pnpm --filter @myapp/api db:migrate",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "oxlint --node-plugin packages",
    "lint:fix": "oxlint --fix --node-plugin packages"
  },
  "devDependencies": {
    "oxlint": "^1.69.0",
    "typescript": "~5.9.3"
  }
}
